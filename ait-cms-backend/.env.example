# Database Connection
DATABASE_URL="postgresql://user:password@localhost:5432/ait_cms?schema=public"

# Authentication Secrets
# Generate strong secrets for production (e.g., using `openssl rand -base64 32`)
JWT_SECRET="super-secret-jwt-key"
JWT_REFRESH_SECRET="super-secret-refresh-key"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Server Configuration
PORT=3000
NODE_ENV="development" # Set to "production" for deployment

# Redis Configuration (Optional - for Caching/Queues)
REDIS_HOST="localhost"
REDIS_PORT=6379

# File Storage (Optional - Local by default)
# STORAGE_TYPE="local" # or "s3"
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION=""
# AWS_BUCKET_NAME=""
